# Personal Notes

Minimal Flask app for taking personal notes. The SQLite database `notes.db` will be created on first run. Do not commit `notes.db`.

Quick start (recommended in a virtual environment):

1. Install dependencies:

   pip install -r requirements.txt

2. Run the app:

   python app.py

3. Open http://127.0.0.1:5000 in your browser.
